{
  "name": "hw5-portfolio",
  "version": "1.0.0",
  "description": "Sarah Mitchell's Portfolio - HW5",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy && pagefind --site _site",
    "debug": "DEBUG=* eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.0.0"
  }
}